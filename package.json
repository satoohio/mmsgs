{
  "name": "mmsgs",
  "version": "1.0.0",
  "private": true,
  "description": "mmsgs — веб-мессенджер: авторизация, друзья, личные и групповые чаты в реальном времени",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "seed": "node server/seed.js",
    "reset": "node server/seed.js --reset",
    "test": "node --test tests/"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}
